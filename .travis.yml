language: python

python:
  - "2.7"

install:
  - "pip install codecov"
  - "pip install -r requirements.txt"

before_script:
 - npm install -g grunt

script:
  grunt.js /templates/test.html


after_success:
  - codecov
